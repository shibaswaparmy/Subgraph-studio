{
  "name": "shibarium-419",
  "license": "UNLICENSED",
  "scripts": {
    "lint": "eslint --ext .ts --ext .js --ext .json .eslintrc.js src abis",
    "lint:fix": "eslint --ext .ts --ext .js --ext .json .eslintrc.js src abis --fix",
    "graph": "graph",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ shibarium-419",
    "create-local": "graph create --node http://localhost:8020/ shibarium-419",
    "remove-local": "graph remove --node http://localhost:8020/ shibarium-419",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://127.0.0.1:5001 shibarium-419"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.25",
    "@graphprotocol/graph-cli": "^0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.25.3",
    "@typescript-eslint/eslint-plugin": "^4.10.0",
    "@typescript-eslint/parser": "^4.10.0",
    "eslint": "^7.16.0",
    "eslint-plugin-json": "^2.1.2",
    "eslint-plugin-unicorn": "^24.0.0",
    "mustache": "4.0.1",
    "typescript": "^4.1.3"
  }
}

